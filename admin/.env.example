# Copiá este archivo como admin/.env y completá los dos valores a mano.
# admin/.env está en .gitignore y nunca se sube al repo.
# La service role key salta RLS: no la pegues en chats, issues ni capturas.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
